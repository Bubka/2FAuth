language: php

php:
  - "7.2"

before_script:
  - cp .env.travis .env
  - composer self-update
  - composer install --no-interaction
  - php artisan key:generate
  - nvm install node
  - npm install npm@latest -g
  - npm install

script:
  - npm run dev
  - vendor/bin/phpunit